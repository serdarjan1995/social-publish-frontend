<template>
  <v-card :max-width="maxWidth" class="neu-glow private-notification-card-area">
    <v-card-title class="mt-0 mb-5 pa-5">
      <v-row class="mx-0" align="center" justify="space-between">
        <v-card-title class="text-caption-1 pa-0">{{ title }}</v-card-title>
      </v-row>
    </v-card-title>
    <v-card-text class="mt-0 pt-0">
      <v-row class="mx-0" align="center" justify="center">
        <template v-for="(item, i) in list">
          <list-item
            v-bind="item"
            :items="item"
            :key="`${item.title}-flexitem-${i}`"
            :class="i < list.length - 1 ? 'mb-4' : ''"
            :align-items="alignItems"
          />
        </template>
      </v-row>
    </v-card-text>
  </v-card>
</template>

<script>
import ListItem from "./Partials/FlexListItem";

export default {
  components: { ListItem },
  props: {
    maxWidth: {
      type: [String, Number],
      default: "",
    },
    list: {
      type: Array,
      default: () => [],
    },
    title: {
      type: String,
      default: null,
    },

    alignItems: {
      type: String,
      default: "center",
    },
  },
};
</script>
<style>
  .private-notification-card-area {
    height: 500px !important;
    overflow-y: auto;
  }
</style>
